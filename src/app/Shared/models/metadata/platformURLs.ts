/*
export interface PlatformURLs {
  //Storage services
  onedrive?: URL;
  dropbox?: URL;

  //Information URLs
  official?: URL;
  itunes?: URL;
  spotify?: URL;
  tidal?: URL;
  beatport?: URL;
  beatsource?: URL;
}
*/

export interface PlatformURLs {
  //Storage services
  onedrive?: string;
  dropbox?: string;

  //Information strings
  official?: string;
  itunes?: string;
  spotify?: string;
  tidal?: string;
  beatport?: string;
  beatsource?: string;
}
